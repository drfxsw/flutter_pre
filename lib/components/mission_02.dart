import 'package:flutter/material.dart';

class Mission02Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mission 02'),
      ),
      body: Center(
        child: Text('The Second Mission Screen'),
      ),
    );
  }
}