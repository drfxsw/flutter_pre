import 'package:flutter/material.dart';

class Mission03Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mission 03'),
      ),
      body: Center(
        child: Text('The third Mission Screen'),
      ),
    );
  }
}