
import 'package:flutter/material.dart';

class Mission05Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mission 05'),
      ),
      body: Center(
        child: Text('The Fifth Mission Screen'),
      ),
    );
  }
}